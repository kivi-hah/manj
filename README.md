<div align="center">

   
# **Manj**

**Бесплатный десктопный музыкальный плеер-агрегатор.**
Яндекс Музыка и SoundCloud (скоро и Spotify) — в одном интерфейсе, без подписок, рекламы и ограничений.

`v0.1.3 · бета` · Windows 10/11 · Tauri 2 + React + Rust

</div>

---

## Возможности

- **Единая медиатека** — лайки и аудиотеки Яндекса и VK в одной библиотеке (до 8000 треков)
- **«Моя волна»** — бесконечный персональный поток с миксом 70/30: 70% под твой вкус, 30% новинок для открытия
- **Режимы волны** — «Популярное», «Любимое», «Незнакомое» и **«Волна по треку»** (похожее на любой трек)
- **Живой интерфейс** — плеер и полноэкранный режим перекрашиваются в цвета обложки играющего трека
- **Страницы артистов** — обложка, описание и популярные треки в фирменном стиле
- **Единый поиск** по всем подключённым сервисам
- **Приватность** — токены и сессии хранятся в системном хранилище ОС (Windows Credential Manager), веб-слой их никогда не видит
- **Локальный режим** — если сервисы недоступны, приложение продолжает работать на локальном каталоге
- **Настройки** — 3 цветовые схемы логотипа, громкость, повтор очереди, автозапуск, плавные переходы, горячие клавиши

## Скриншоты

# Главная и Библиотека
<img width="1920" height="1080" alt="{648B0FB1-2524-4D10-9220-66B624DBDA7E}" src="https://github.com/user-attachments/assets/12ec6037-215f-456c-9508-cd762ba19838" />
<img width="1920" height="1080" alt="{183DF2A0-0C9F-4E20-9A13-285BDEF574B5}" src="https://github.com/user-attachments/assets/a746d49f-684b-4ca5-9d11-d3b274934948" />


# Волна (режимы) и Полноэкранный плеер 
<img width="1920" height="1080" alt="{DE135903-4CFE-46DE-AFA9-4BAC3C2D22CD}" src="https://github.com/user-attachments/assets/c81e1a89-fd27-4417-9083-b9c2ab953f51" />
<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/33534d93-6c5c-4b2b-b8d9-0ca80094986a" />



# Установка

1. Открой [**Releases**](https://github.com/kivi-hah/manj/releases)
2. Скачай `Manj_0.1.2_x64_en-US.msi` (установщик)
3. Установи и запусти — **ничего дополнительно ставить не нужно**

> Windows 10/11 уже содержит всё необходимое (WebView2).

## Первый запуск

1. Приложение сразу работает в **демо-режиме** — жми Play на синей карточке и слушай.
2. Чтобы слушать **свою музыку**: `Настройки → Интеграции`
3. Лайкай треки, включай волну — алгоритм подстраивается под тебя в реальном времени: скип понижает вес похожего, лайк и дослушивание повышают.

## Как работает «Моя волна»

Алгоритм ведёт **вкусовой профиль** — веса артистов и жанров, которые:

- растут от лайков (+3) и дослушиваний (+1);
- падают от ранних скипов (−1.6);
- усиливаются вдвойне, когда «незнакомый» трек зашёл (+3.5);
- затухают на 3% каждые 12 часов — интересы меняются, и волна следует за ними.

Каждая партия очереди собирается как **70% «под вкус» + 30% открытий**, чередуется группами и подгружается на лету — за 3 трека до конца очереди. Вся история хранится в локальной SQLite, профиль переживает перезапуск.

## Сборка из исходников (только версия 0.1.1)

Нужны: [Node.js 18+](https://nodejs.org), [Rust](https://rustup.rs), MSVC Build Tools (C++ workload).

```bash
git clone https://github.com/kivi-hah/manj.git
cd manj
npm install
npm run tauri:dev     # разработка
npm run tauri:build   # exe и установщик → src-tauri/target/release/bundle/
```

## Стек и архитектура

| Слой | Технологии |
|---|---|
| UI | React 18 + TypeScript + Tailwind CSS, Material Symbols, Roboto Flex |
| Состояние | Zustand |
| Аудио | HTML5 Audio + hls.js (MP3 / HLS), предзагрузка следующего трека |
| Ядро | Rust (Tauri 2): keyring-хранилище, API-прокси, SQLite, подпись потоков |
| Данные | SQLite — история прослушиваний, вкусовой профиль, настройки |

```
src/
├── services/     # адаптеры сервисов (единый IMusicService)
├── wave/         # движок «Моей волны»: профиль, микс 70/30, discovery
├── player/       # аудиодвижок + очередь
├── stores/       # состояние (zustand)
└── components/   # UI по макетам

src-tauri/src/
├── secure_store.rs   # токены в keyring (чтение — только внутри Rust)
├── api_proxy.rs      # запросы к сервисам с подстановкой токенов
├── oauth.rs          # вход через встроенный браузер с перехватом
├── db.rs             # SQLite: история и профиль
└── stream_proxy.rs   # схема stream:// для аудио с Range-поддержкой
```

## Дорожная карта

- [ ] Плейлисты: просмотр и воспроизведение целиком
- [ ] Тексты песен
- [ ] macOS / Linux сборки

## Дисклеймер

Manj — **неофициальное** приложение и не связано с Яндексом и SoundCloud (скоро и Spotify).
Для прослушивания полной версии треков необходима активная подписка соответствующего сервиса — без неё сервисы отдают короткие превью.

## Лицензия

MIT
